import React from 'react'
import PropTypes from 'prop-types'

const Education = ({ items }) => <p>Education List</p>

Education.propTypes = {
  items: PropTypes.object.isRequired
}

export default Education
