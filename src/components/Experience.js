import React from 'react'
import PropTypes from 'prop-types'

const Experience = ({ items }) => <div>Experience</div>

Experience.proptoType = {
  items: PropTypes.object.isRequired
}

export default Experience
